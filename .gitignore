# État local du mineur (jamais versionné — propre à chaque machine)
miner-state.json
.axecube-config
leaderboard-data.json
banque-*.json

# Dépendances
node_modules/
leaderboard-netlify/node_modules/
leaderboard-netlify/.netlify/

# Fichiers système
.DS_Store
Thumbs.db

# Éditeurs
.vscode/
.idea/
*.swp

# Outils admin personnels (jamais distribués avec AXECUBE, contiennent des identifiants)
axecube-admin.command
netlify-blobs-clean.js
.env
.env.*

# Artefacts locaux (app compilée, logs, archives — pas du code source)
AXECUBE Desktop.app/
axecube.log
leaderboard-netlify.zip
AxecubeMenuBar
treasury-devnet.json
